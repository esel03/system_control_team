meta:
  environment: python
  toolchain:
    name: pip
    version: "3.13"
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  command: uvicorn main.main:app --host=0.0.0.0 --port=80
  persistenceMount: /data
  containerPort: "80"
