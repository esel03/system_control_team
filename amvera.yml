meta:
  environment: python
  toolchain:
    name: pip
    version: "3.13"
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  command: PORT=80 bash start.sh
  persistenceMount: /data
  containerPort: "80"
